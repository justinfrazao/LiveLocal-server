MongoDB Features to use
  Supports geo-spatial queries to find all the documents near a geographic location
  Supports text search queries using text indexes on fields to be searched

Jest unit testing

Events collection
  organizer id
Profile collection
  array of event id's
